const score=400

const balance=new Number(500)  //here we defined its type that is number

console.log(score);
console.log(typeof score);
console.log(balance);

console.log(balance.toString());  // prints the balance and then change it to string

console.log(balance.toFixed(3));  // prints the balance till 3 decimal places
